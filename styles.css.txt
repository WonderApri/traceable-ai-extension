.tai-verify-btn {
  position: absolute;
  top: 10px;
  right: 10px;
  background: rgba(29, 155, 240, 0.9); /* Twitter Blue */
  color: white;
  border: none;
  border-radius: 9999px;
  padding: 6px 14px;
  font-size: 12px;
  font-weight: bold;
  cursor: pointer;
  z-index: 100;
  box-shadow: 0 4px 6px rgba(0,0,0,0.1);
  transition: transform 0.2s;
}

.tai-verify-btn:hover {
  background: #1a8cd8;
  transform: scale(1.05);
}